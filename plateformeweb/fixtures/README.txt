The fixtures were produced with the following commands after creating the accounts and populating the initial data:

    ./manage.py dumpdata --format=json auth > plateformeweb/fixtures/001_eventtypes.json

NOTE: what about i18n/l10n? Maybe put this in the code instead?
